const employee = {
    name: 'Raja Rani',
    designation: 'QA',
    salary: '20000',
    experience: 1
}

const key = Object.keys(employee)
// console.log(key);

const value =Object.values(employee)
// console.log(value);

const entry = Object.entries(employee)
console.log(entry);

